# Versions are pinned to releases that ship binary wheels for Python 3.14
# (the supported interpreter). Bumping these is what keeps a single
# `pip install -r requirements.txt` resolving without a source build.

# ── Web framework ─────────────────────────────────────────────────
fastapi==0.137.2          # pulls in starlette automatically
uvicorn[standard]==0.49.0
python-multipart==0.0.32  # multipart/form-data for the file-upload endpoints

# ── Auth (multi-tenant: JWT + password hashing) ───────────────────
PyJWT==2.13.0
passlib==1.7.4           # pbkdf2_sha256 password hashing (pure-python)

# ── HTTP client (FastAPI TestClient in the test suite) ────────────
httpx==0.28.1

# ── Data processing ───────────────────────────────────────────────
numpy==2.4.6
openpyxl==3.1.5          # read/write .xlsx for Excel creator import/export

# ── Visualisation (experiments / report figures) ──────────────────
matplotlib==3.11.0

# ── Testing ───────────────────────────────────────────────────────
pytest==9.1.1
pytest-cov==7.1.0
